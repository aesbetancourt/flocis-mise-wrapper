# Emulator state and upgrade backups
floci-lab/data/
floci-lab/backups/
floci-lab/.data-restore/

# OpenTofu
floci-lab/.terraform/
floci-lab/*.tfstate
floci-lab/*.tfstate.*
floci-lab/crash.log

# Local overrides
.env.local
.DS_Store
